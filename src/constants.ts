export enum ROUTES {
  CREATE_EVENT = '/event/create',
  HELP = '/help',
  HOME = '/',
  SEARCH = '/search',
}

// Supported languages
export enum SUPPORTED_LANGUAGES {
  FI = 'fi',
  SV = 'sv',
  EN = 'en',
}

export const MAIN_CONTENT_ID = 'maincontent';
