import { MAX_PAGE_SIZE } from '../../constants';

export const TEST_PUBLISHER_ID = 'publisher:1';

export const TEST_DATA_SOURCE = 'datasource';

export const MAX_OGRANIZATIONS_PAGE_SIZE = MAX_PAGE_SIZE;
