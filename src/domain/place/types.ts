export type PlaceFields = {
  addressLocality: string;
  atId: string;
  dataSource: string;
  id: string;
  name: string;
  nEvents: number;
  placeUrl: string;
  publisher: string;
  streetAddress: string;
};
