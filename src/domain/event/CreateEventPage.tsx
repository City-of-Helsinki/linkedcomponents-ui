import React from 'react';

import PageWrapper from '../app/layout/PageWrapper';

const CreateEventPage: React.FC = () => {
  return <PageWrapper title="createEventPage.pageTitle"></PageWrapper>;
};

export default CreateEventPage;
